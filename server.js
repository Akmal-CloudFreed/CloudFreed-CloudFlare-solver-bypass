import CloudFreed from "./CloudFreed.js";

const cf_clearence =  CloudFreed("bloxmoon.com")
const cf_clearences = await CloudFreed("bloxmoon.com")
console.log(cf_clearence, cf_clearences, cf_clearence.cf_clearence)